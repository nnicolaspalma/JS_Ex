/*

// Arrays Elementos Primitivos

const num = [1, 2, 3, 4];

// Retorna o Primeiro indice que pode ser encontrado no array / Ou -1 se não for encontrado
console.log(num.indexOf(3))

// Retorna o ultimo indice que pode ser encontrado no array / Ou -1 se não for encontrado
console.log(num.lastIndexOf(3));

// Formula
console.log(num.indexOf(2) !== -1);

// Formula Simplificada
console.log(num.includes(2))

*/