/*

// Combinando E Cortando Arrays

const primeiro = [1, 2, 3];
const segundo = [4, 5, 6];

// Combinar
const combinacao = primeiro.concat(segundo);
console.log(combinacao);

// Dividir
const cortado = combinacao.slice(1, 3);
console.log(cortado);

// Spread ( ES6 ) - Me permite fazer modificações
const combinado1 = [...primeiro, 'A', ...segundo, 'B'];
console.log(combinado1);

// Clone
const clonado = [...combinado1];
console.log(clonado);

*/