/*

// for
// while
// Do-While

// For-in

const pessoa = {
    nome: 'Palma',
    idade: 17
};

//key-value (Nome= Key, Palma = value)

for (let chave in pessoa) {
    console.log(chave, pessoa['nome']);
}

const cores = ['Vermelho', 'Azul', 'Verde'];

/*
for (let indice in cores) {
    console.log(indice, cores[indice]);
}


// for-of
for (let cor of cores) {
    console.log(cor);
}

*/